let heap=[null]
function insert(num)
{
    heap.push(num)
    if(heap.length>2)
    {
        idx= heap.length-1
        while(heap[idx]<heap[Math.floor(idx/2)])
        {
            if(idx>1)
        }
    }
}